name: alleleome
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python==3.10
  - pip
  - pip:
    - git+https://github.com/biosustain/pankb_data_prep.git@nova
    - ratelimiter==1.2.0.post
